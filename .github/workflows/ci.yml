name: Проверка сборки

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Получить код
        uses: actions/checkout@v4
      - name: Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package.json
      - name: Установить зависимости
        run: npm install --no-audit --no-fund
      - name: Проверить History Bootstrap и рекомендации
        run: npm run test:history-bootstrap
      - name: Проверить AI routing
        run: npm run test:ai-routing
      - name: Проверить Communicative Core
        run: npm run test:communicative-core
      - name: Проверить Course Baseline и Module Brief
        run: npm run test:course-planning-guidance
      - name: Проверить выбор страниц источника
        run: npm run test:source-resolution
      - name: Проверить production build
        run: npm run build
