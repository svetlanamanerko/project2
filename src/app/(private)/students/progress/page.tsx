import { Target } from 'lucide-react'; import { EmptyState } from '@/components/EmptyState'; import { getStudentProgressRows } from '@/lib/data'; import { ProgressRows } from './ProgressRows';
export default async function StudentProgressPage(){const rows=await getStudentProgressRows();return <><header className="page-head"><div><p className="eyebrow">Фактический путь каждого ученика</p><h1>Прогресс учеников</h1><p className="muted">Один раз укажите реальную текущую точку. Старые уроки задним числом не создаются.</p></div><div className="round-icon"><Target/></div></header><section className="panel">{rows.length?<ProgressRows rows={rows}/>:<EmptyState title="Нет активных маршрутов" text="Сначала свяжите учеников с курсами."/>}</section></>}
